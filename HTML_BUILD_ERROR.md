# Error de generación del HTML distribuible

La generación automática ha fallado. Este fichero se versiona temporalmente para diagnosticar el parche real aplicado a los payloads.

```text
Traceback (most recent call last):
  File "/home/runner/work/workforce-planning-validator/workforce-planning-validator/scripts/build_distributable_html.py", line 290, in <module>
    main()
  File "/home/runner/work/workforce-planning-validator/workforce-planning-validator/scripts/build_distributable_html.py", line 284, in main
    html = build_html(payload)
           ^^^^^^^^^^^^^^^^^^^
  File "/home/runner/work/workforce-planning-validator/workforce-planning-validator/scripts/build_distributable_html.py", line 274, in build_html
    payload, source = patched_payload(payload)
                      ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/work/workforce-planning-validator/workforce-planning-validator/scripts/build_distributable_html.py", line 264, in patched_payload
    source = patch_workforce_insights(source)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/work/workforce-planning-validator/workforce-planning-validator/scripts/workforce_insights_html_patch.py", line 179, in patch_workforce_insights
    source = _insert_mix_tab(source)
             ^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/runner/work/workforce-planning-validator/workforce-planning-validator/scripts/workforce_insights_html_patch.py", line 34, in _insert_mix_tab
    raise ValueError(
ValueError: Parche HTML de insights: no se encontró la lista de pestañas para insertar Mix de plantilla

--- CONTEXTO DE NAVEGACION DEL HTML BASE ---

[Fines de semana] coincidencias=7
ring(b.store) ? -1 : String(a.store) > String(b.store) ? 1 :\n       String(a.person) < String(b.person) ? -1 : String(a.person) > String(b.person) ? 1 :\n       a.month < b.month ? -1 : a.month > b.month ? 1 :\n       a.start - b.start || (a.type < b.type ? -1 : a.type > b.type ? 1 : 0)));\n    return inc;\n  }\n\n  // ---- Fines de semana / mes -----------------------------------------------\n  function weekendCounts(year, month, workedSet) {\n    const daysInMonth = new Date(Date.UTC(year, month, 0)).getUTCDate();\n    let freeSat = 0, freeSun = 0, complete = 0;\n    for (let d = 1; d <= daysInMonth; d++) {\n      const day = dayFromYMD(year, month, d);\n      const wd = weekdayISO(day);\n      if (wd === 5) { // sábado\n        if (!workedSet.has(day)) freeSat++;\n        const nxt = day + 1;\n        if (!workedSet.has(day) && dayYMD(nxt).m
---
da {store} · Analisis generado {stamp} · {wf}":"Store {store} · Analysis generated {stamp} · {wf}",\n  // Tabs\n  "Resumen":"Summary","Restricciones":"Constraints","Horas contractuales":"Contracted hours",\n  "Cobertura diaria":"Daily coverage","Balance mañana/tarde":"Morning/afternoon balance",\n  "Ausencias":"Absences","Fines de semana":"Weekends","Metodología":"Methodology","Metodologia":"Methodology",\n  // Incidents\n  "Mas de 5 dias consecutivos":"More than 5 consecutive days",\n  "Turno superior a 7,5 h":"Shift longer than 7.5 h",\n  "Turno inferior a 4 h":"Shift shorter than 4 h",\n  "Descanso inferior a 11 h":"Rest shorter than 11 h",\n  // Days of week\n  "Lunes":"Monday","Martes":"Tuesday","Miercoles":"Wednesday","Miércoles":"Wednesday",\n  "Jueves":"Thursday","Viernes":"Friday","Sabado":"Saturday","Sábado":"Saturday","Domingo":"
---
 employees; days with no absences are kept at zero.",\n  "Ausencias por tipo":"Absences by type",\n  "Cuenta registros de ausencia por tipo. Dos tipos distintos en una misma fecha se mantienen como dos registros.":"Counts absence records by type. Two different types on the same date are kept as two records.",\n\n  // ---- Fines de semana ----\n  "Fines de semana y rotación de descansos":"Weekends and rest rotation",\n  "Descargar tabla":"Download table","Tabla":"Table","Solo semanas con planificación":"Only weeks with planning",\n  "Semanas activas":"Active weeks","Semanas con al menos un turno planificado":"Weeks with at least one planned shift","Días con ausencias":"Days with absences","Dentro de semanas activas":"Within active weeks",\n  "Reglas de descanso de fin de semana":"Weekend rest rules","Introduce por teclado el mínimo exigi
---
 absences are kept at zero.",\n  "Ausencias por tipo":"Absences by type",\n  "Cuenta registros de ausencia por tipo. Dos tipos distintos en una misma fecha se mantienen como dos registros.":"Counts absence records by type. Two different types on the same date are kept as two records.",\n\n  // ---- Fines de semana ----\n  "Fines de semana y rotación de descansos":"Weekends and rest rotation",\n  "Descargar tabla":"Download table","Tabla":"Table","Solo semanas con planificación":"Only weeks with planning",\n  "Semanas activas":"Active weeks","Semanas con al menos un turno planificado":"Weeks with at least one planned shift","Días con ausencias":"Days with absences","Dentro de semanas activas":"Within active weeks",\n  "Reglas de descanso de fin de semana":"Weekend rest rules","Introduce por teclado el mínimo exigido en el periodo analiza
---
 morningCutoff:"11:00", afternoonCutoff:"14:00", wkendMin:null, wkendMax:null, wkendReqFull:1, wkendReqSat:1, wkendReqSun:1, wkendAlerts:true, wkendDiag:0, wkendMapQuery:"", wkendMapAlerts:false },\n};\nconst TAB_KEYS=["Resumen","Restricciones","Horas contractuales","Cobertura diaria","Balance mañana/tarde","Ausencias","Fines de semana","Metodología"];\nfunction tabNames(){ return TAB_KEYS.map(k=>t(k)); }\n\nconst D = WFV; // motor\nfunction di(iso){ return D.parseDay(iso); }        // ISO -> day int\nfunction dmy(dayInt){ return D.fmtDMY(dayInt); }\nfunction ddmm(dayInt){ const {d,m}=D.dayYMD(dayInt); return `${String(d).padStart(2,"0")}/${String(m).padStart(2,"0")}`; }\n\n\n/* ---------- Carga y combinación de uno o dos meses ---------- */\nfunction parsePlanningText(text, filename){\n  let data;\n  try{ data=JSON.parse(String(text).replace
---
fecha se mantienen como dos registros."));\n    const g=groupBy(F.absences,a=>a.tipo_ausencia);\n    const items=[...g.entries()].map(([label,rs])=>({label,value:rs.length,color:"#7c3aed"})).sort((a,b)=>a.value-b.value);\n    h+=chartBarH(items,{h:Math.max(300,40*items.length)})+`</div>`;\n  }\n  return h;\n}\n\n/* ---------- Fines de semana ---------- */\nfunction prepWeekend(F){\n  const dates=[...F.dataSet].sort((a,b)=>a-b); const dateSet=F.dataSet;\n  // empleados: applicable máximo (o null si algún null)\n  const g=groupBy(F.weekly,w=>w.id_tienda+"|"+w.personId);\n  const employees=[...g.values()].map(rs=>{ const vals=rs.map(r=>r.applicableWorkingHours);\n    const app = vals.some(v=>v==null)?null:Math.max(...vals.map(Number));\n    return {id_tienda:rs[0].id_tienda,personId:rs[0].personId,app}; });\n  const worked=new Set(F.shifts.map(s=>
---
emp.id_tienda,personId:emp.personId,app:emp.app,Mes:`${Y}-${String(M).padStart(2,"0")}`,\n        fs_eval:pairs.length,fs_libres:freeWk,sab_eval:sats.length,sab_libres:freeSat,dom_eval:suns.length,dom_libres:freeSun}); });\n  });\n  return {monthly,detail};\n}\nfunction renderWeekends(F){\n  let h=`<div class="subhead">${t("Fines de semana y rotación de descansos")}</div>`+help(t("Un fin de semana libre exige no trabajar ni el sábado ni el domingo. Sábados y domingos libres también se cuentan por separado, por empleado y mes."));\n  const {monthly,detail}=prepWeekend(F);\n  if(!monthly.length) return h+callout("warning",t("No hay información suficiente para calcular fines de semana."));\n  const availHours=monthly.map(m=>m.app).filter(v=>v!=null&&isFinite(v));\n  const obsMax=availHours.length?Math.max(...availHours):40; const limit=Math.
---

[weekends] coincidencias=9
an absence overlaps a shift in the selected source.",\n  "Registros de ausencia":"Absence records","Estados VALIDATED o APPROVED":"VALIDATED or APPROVED status",\n  "Empleados afectados":"Affected employees","Personas con alguna ausencia":"People with at least one absence",\n  "Media fines de semana libres":"Average free weekends","Por empleado-mes":"Per employee-month",\n  "Sin fin de semana completo":"With no full weekend off","Registros empleado-mes":"Employee-month records",\n  "Calendario diario de ausencias":"Daily absence calendar",\n  "Cada celda muestra empleados únicos ausentes; los días sin ausencias se conservan con cero.":"Each cell shows unique absent employees; days with no absences are kept at zero.",\n  "Ausencias por tipo":"Absences by type",\n  "Cuenta registros de ausencia por tipo. Dos tipos distintos en una misma f
---
n active weeks",\n  "Reglas de descanso de fin de semana":"Weekend rest rules","Introduce por teclado el mínimo exigido en el periodo analizado. Un valor 0 desactiva la regla.":"Type the minimum required for the analysed period. A value of 0 disables the rule.",\n  "Mínimo fines de semana completos libres":"Minimum full weekends off","Mínimo sábados libres":"Minimum Saturdays off","Mínimo domingos libres":"Minimum Sundays off","Regla activa":"Active rule","Mínimo":"Minimum",\n  "Con alguna incidencia":"With an issue","Incumplen fines completos":"Fail full-weekend rule","Incumplen sábados":"Fail Saturday rule","Incumplen domingos":"Fail Sunday rule",\n  "Incidencias según las reglas introducidas":"Issues under the entered rules","Las pestañas se recalculan inmediatamente al cambiar cualquiera de los tres mínimos.":"The tabs recalcula
---
rday rule","Incumplen domingos":"Fail Sunday rule",\n  "Incidencias según las reglas introducidas":"Issues under the entered rules","Las pestañas se recalculan inmediatamente al cambiar cualquiera de los tres mínimos.":"The tabs recalculate immediately when any of the three minimums changes.",\n  "Fines completos":"Full weekends","Sábados":"Saturdays","Domingos":"Sundays","Mostrar solo empleado-mes con alguna incidencia":"Show only employee-month rows with an issue",\n  "Las semanas completamente vacías de planificación se excluyen de cobertura, ausencias, balance y fines de semana.":"Completely empty planning weeks are excluded from coverage, absences, balance and weekend analysis.","Reglas de fin de semana":"Weekend rules","fines completos":"full weekends","sábados libres":"Saturdays off","domingos libres":"Sundays off",\n  "Un fi
---
ployee-month rows with an issue",\n  "Las semanas completamente vacías de planificación se excluyen de cobertura, ausencias, balance y fines de semana.":"Completely empty planning weeks are excluded from coverage, absences, balance and weekend analysis.","Reglas de fin de semana":"Weekend rules","fines completos":"full weekends","sábados libres":"Saturdays off","domingos libres":"Sundays off",\n  "Un fin de semana libre exige no trabajar ni el sábado ni el domingo. Sábados y domingos libres también se cuentan por separado, por empleado y mes.":"A free weekend requires not working either Saturday or Sunday. Free Saturdays and Sundays are also counted separately, per employee and month.",\n  "No hay información suficiente para calcular fines de semana.":"Not enough information to calculate weekends.",\n  "Horas contractuales mínimas":
---
y domingos libres también se cuentan por separado, por empleado y mes.":"A free weekend requires not working either Saturday or Sunday. Free Saturdays and Sundays are also counted separately, per employee and month.",\n  "No hay información suficiente para calcular fines de semana.":"Not enough information to calculate weekends.",\n  "Horas contractuales mínimas":"Minimum contracted hours","Horas contractuales máximas":"Maximum contracted hours",\n  "Las horas mínimas no pueden ser superiores a las horas máximas.":"The minimum hours cannot be higher than the maximum hours.",\n  "No hay empleados que cumplan el rango contractual seleccionado.":"No employees match the selected contract range.",\n  "Empleados analizados":"Employees analysed","Contrato entre {a} y {b} h":"Contract between {a} and {b} h",\n  "Sin fin de semana libre":"With
---
have both Saturday and Sunday off on the same weekend during the analysed period.",\n  "Todos":"All","Sin sábados":"No Saturdays off","Sin domingos":"No Sundays off","Sin ningún día":"No day off",\n  "No hay empleados en esta categoría.":"No employees in this category.",\n  "Tienda":"Store","Fines completos libres":"Full weekends off","Sábados libres":"Saturdays off","Domingos libres":"Sundays off",\n  "Resumen empleado-mes":"Employee-month summary","Mostrar solo empleado-mes con alguna incidencia":"Show only employee-months with a counter at zero",\n  "Sin alertas":"No alerts","Mes":"Month","Fines libres":"Weekends off","Fines eval.":"Weekends assessed",\n  "Sábados eval.":"Saturdays assessed","Domingos eval.":"Sundays assessed","Alerta":"Alert",\n\n  // ---- Metodología ----\n  "Metodologia":"Methodology","Origen seleccionado:":"Select
---
ras; su valor no se incorpora a la barra de ausencias.":"There are {n} employee-week records with an absence but no reliable hours estimate; their value is not included in the absence bar.",\n  "Buscar empleado en el mapa":"Search employee in the map","Mostrar solo alertas":"Show alerts only",\n  "Fines completos":"Full weekends","Resumen del periodo":"Period summary",\n  "Fines completos: {n} de {total}":"Full weekends: {n} of {total}",\n  "El mapa mantiene fijas las columnas de empleado y contrato. Una fila es alerta si incumple cualquiera de los tres mínimos activos.":"The employee and contract columns remain fixed. Rows with zero full weekends off are treated as alerts.",\n\n  // ---- Excel export ----\n  "Informacion":"Information","Origen analizado":"Source analysed","Descripcion":"Description",\n  "Turnos":"Shifts","Validacion me
---
 with an absence but no reliable hours estimate; their value is not included in the absence bar.",\n  "Buscar empleado en el mapa":"Search employee in the map","Mostrar solo alertas":"Show alerts only",\n  "Fines completos":"Full weekends","Resumen del periodo":"Period summary",\n  "Fines completos: {n} de {total}":"Full weekends: {n} of {total}",\n  "El mapa mantiene fijas las columnas de empleado y contrato. Una fila es alerta si incumple cualquiera de los tres mínimos activos.":"The employee and contract columns remain fixed. Rows with zero full weekends off are treated as alerts.",\n\n  // ---- Excel export ----\n  "Informacion":"Information","Origen analizado":"Source analysed","Descripcion":"Description",\n  "Turnos":"Shifts","Validacion mensual":"Monthly validation","Detalle incidencias":"Incident detail",\n  "Control horas semana
---

[Metodología] coincidencias=4
o {stamp} · {wf}":"Store {store} · Analysis generated {stamp} · {wf}",\n  // Tabs\n  "Resumen":"Summary","Restricciones":"Constraints","Horas contractuales":"Contracted hours",\n  "Cobertura diaria":"Daily coverage","Balance mañana/tarde":"Morning/afternoon balance",\n  "Ausencias":"Absences","Fines de semana":"Weekends","Metodología":"Methodology","Metodologia":"Methodology",\n  // Incidents\n  "Mas de 5 dias consecutivos":"More than 5 consecutive days",\n  "Turno superior a 7,5 h":"Shift longer than 7.5 h",\n  "Turno inferior a 4 h":"Shift shorter than 4 h",\n  "Descanso inferior a 11 h":"Rest shorter than 11 h",\n  // Days of week\n  "Lunes":"Monday","Martes":"Tuesday","Miercoles":"Wednesday","Miércoles":"Wednesday",\n  "Jueves":"Thursday","Viernes":"Friday","Sabado":"Saturday","Sábado":"Saturday","Domingo":"Sunday",\n  // Common\n  "Sin d
---
mmary","Mostrar solo empleado-mes con alguna incidencia":"Show only employee-months with a counter at zero",\n  "Sin alertas":"No alerts","Mes":"Month","Fines libres":"Weekends off","Fines eval.":"Weekends assessed",\n  "Sábados eval.":"Saturdays assessed","Domingos eval.":"Sundays assessed","Alerta":"Alert",\n\n  // ---- Metodología ----\n  "Metodologia":"Methodology","Origen seleccionado:":"Selected source:","El motor lee exclusivamente":"The engine reads exclusively",\n  "Solo se utilizan segmentos con":"Only segments with","La duracion diaria es la suma neta de los segmentos seleccionados.":"The daily length is the net sum of the selected segments.",\n  "Se aplican las mismas reglas: maximo 5 dias consecutivos, entre 4 y 7,5 horas por turno y al menos 11 horas entre jornadas.":"The same rules apply: max. 5 consecutive days, between
---
:00", afternoonCutoff:"14:00", wkendMin:null, wkendMax:null, wkendReqFull:1, wkendReqSat:1, wkendReqSun:1, wkendAlerts:true, wkendDiag:0, wkendMapQuery:"", wkendMapAlerts:false },\n};\nconst TAB_KEYS=["Resumen","Restricciones","Horas contractuales","Cobertura diaria","Balance mañana/tarde","Ausencias","Fines de semana","Metodología"];\nfunction tabNames(){ return TAB_KEYS.map(k=>t(k)); }\n\nconst D = WFV; // motor\nfunction di(iso){ return D.parseDay(iso); }        // ISO -> day int\nfunction dmy(dayInt){ return D.fmtDMY(dayInt); }\nfunction ddmm(dayInt){ const {d,m}=D.dayYMD(dayInt); return `${String(d).padStart(2,"0")}/${String(m).padStart(2,"0")}`; }\n\n\n/* ---------- Carga y combinación de uno o dos meses ---------- */\nfunction parsePlanningText(text, filename){\n  let data;\n  try{ data=JSON.parse(String(text).replace(/^﻿/,"")); }\n  ca
---
"),tc("Fines libres"),tc("Fines eval."),tc("Sábados libres"),tc("Sábados eval."),tc("Domingos libres"),tc("Domingos eval."),tc("Alerta")],rows,{height:480,numCols:["Horas contrato","Fines libres","Fines eval.","Sábados libres","Sábados eval.","Domingos libres","Domingos eval."]})+`</div>`;\n  return h;\n}\n\n/* ---------- Metodología ---------- */\nfunction renderMetodologia(){\n  const src=S.source;\n  return `<div class="subhead">${t("Metodologia")}</div><div class="block md-body">\n  <p><b>${t("Origen seleccionado:")}</b> <code>${esc(src)}</code>. ${t("El motor lee exclusivamente")} <code>dayTimes.${esc(src)}</code>.</p>\n  <ul>\n    <li>${t("Solo se utilizan segmentos con")} <code>hourType = WORK</code>.</li>\n    <li>${t("La duracion diaria es la suma neta de los segmentos seleccionados.")}</li>\n    <li>${t("Se aplican las mismas regl
---

[Metodologia] coincidencias=6
ore} · Analysis generated {stamp} · {wf}",\n  // Tabs\n  "Resumen":"Summary","Restricciones":"Constraints","Horas contractuales":"Contracted hours",\n  "Cobertura diaria":"Daily coverage","Balance mañana/tarde":"Morning/afternoon balance",\n  "Ausencias":"Absences","Fines de semana":"Weekends","Metodología":"Methodology","Metodologia":"Methodology",\n  // Incidents\n  "Mas de 5 dias consecutivos":"More than 5 consecutive days",\n  "Turno superior a 7,5 h":"Shift longer than 7.5 h",\n  "Turno inferior a 4 h":"Shift shorter than 4 h",\n  "Descanso inferior a 11 h":"Rest shorter than 11 h",\n  // Days of week\n  "Lunes":"Monday","Martes":"Tuesday","Miercoles":"Wednesday","Miércoles":"Wednesday",\n  "Jueves":"Thursday","Viernes":"Friday","Sabado":"Saturday","Sábado":"Saturday","Domingo":"Sunday",\n  // Common\n  "Sin datos":"No data","No hay dato
---
 empleado-mes con alguna incidencia":"Show only employee-months with a counter at zero",\n  "Sin alertas":"No alerts","Mes":"Month","Fines libres":"Weekends off","Fines eval.":"Weekends assessed",\n  "Sábados eval.":"Saturdays assessed","Domingos eval.":"Sundays assessed","Alerta":"Alert",\n\n  // ---- Metodología ----\n  "Metodologia":"Methodology","Origen seleccionado:":"Selected source:","El motor lee exclusivamente":"The engine reads exclusively",\n  "Solo se utilizan segmentos con":"Only segments with","La duracion diaria es la suma neta de los segmentos seleccionados.":"The daily length is the net sum of the selected segments.",\n  "Se aplican las mismas reglas: maximo 5 dias consecutivos, entre 4 y 7,5 horas por turno y al menos 11 horas entre jornadas.":"The same rules apply: max. 5 consecutive days, between 4 and 7.5 hours per
---
c("Sábados libres"),tc("Sábados eval."),tc("Domingos libres"),tc("Domingos eval."),tc("Alerta")],rows,{height:480,numCols:["Horas contrato","Fines libres","Fines eval.","Sábados libres","Sábados eval.","Domingos libres","Domingos eval."]})+`</div>`;\n  return h;\n}\n\n/* ---------- Metodología ---------- */\nfunction renderMetodologia(){\n  const src=S.source;\n  return `<div class="subhead">${t("Metodologia")}</div><div class="block md-body">\n  <p><b>${t("Origen seleccionado:")}</b> <code>${esc(src)}</code>. ${t("El motor lee exclusivamente")} <code>dayTimes.${esc(src)}</code>.</p>\n  <ul>\n    <li>${t("Solo se utilizan segmentos con")} <code>hourType = WORK</code>.</li>\n    <li>${t("La duracion diaria es la suma neta de los segmentos seleccionados.")}</li>\n    <li>${t("Se aplican las mismas reglas: maximo 5 dias consecutivos, entre 4 y
---
s eval."),tc("Alerta")],rows,{height:480,numCols:["Horas contrato","Fines libres","Fines eval.","Sábados libres","Sábados eval.","Domingos libres","Domingos eval."]})+`</div>`;\n  return h;\n}\n\n/* ---------- Metodología ---------- */\nfunction renderMetodologia(){\n  const src=S.source;\n  return `<div class="subhead">${t("Metodologia")}</div><div class="block md-body">\n  <p><b>${t("Origen seleccionado:")}</b> <code>${esc(src)}</code>. ${t("El motor lee exclusivamente")} <code>dayTimes.${esc(src)}</code>.</p>\n  <ul>\n    <li>${t("Solo se utilizan segmentos con")} <code>hourType = WORK</code>.</li>\n    <li>${t("La duracion diaria es la suma neta de los segmentos seleccionados.")}</li>\n    <li>${t("Se aplican las mismas reglas: maximo 5 dias consecutivos, entre 4 y 7,5 horas por turno y al menos 11 horas entre jornadas.")}</li>\n    <li>
---
riginal (paridad byte a byte en turnos, incidencias, horas semanales, balance y ausencias).")}</p>\n  </div>`;\n}\n\n/* ============================= Render orchestration =================== */\nconst RENDERERS=[renderResumen,renderRestricciones,renderHoras,renderCobertura,renderBalance,renderAusencias,renderWeekends,renderMetodologia];\nfunction renderTab(){\n  TABLE_EXPORTS.clear(); TABLE_SEQ=0;\n  const F=S.frames; const panel=document.getElementById("panel");\n  panel.innerHTML = S.tab===7? renderMetodologia() : RENDERERS[S.tab](F);\n  wireTabControls();\n}\nfunction filterWeekendMapRows(){\n  const tableEl=document.getElementById("weekendMapTable"); if(!tableEl) return;\n  const q=String(S.f.wkendMapQuery||"").trim().toLowerCase();\n  tableEl.querySelectorAll("tbody tr").forEach(row=>{\n    const matchesText=!q || String(row.dataset.search
---
========= */\nconst RENDERERS=[renderResumen,renderRestricciones,renderHoras,renderCobertura,renderBalance,renderAusencias,renderWeekends,renderMetodologia];\nfunction renderTab(){\n  TABLE_EXPORTS.clear(); TABLE_SEQ=0;\n  const F=S.frames; const panel=document.getElementById("panel");\n  panel.innerHTML = S.tab===7? renderMetodologia() : RENDERERS[S.tab](F);\n  wireTabControls();\n}\nfunction filterWeekendMapRows(){\n  const tableEl=document.getElementById("weekendMapTable"); if(!tableEl) return;\n  const q=String(S.f.wkendMapQuery||"").trim().toLowerCase();\n  tableEl.querySelectorAll("tbody tr").forEach(row=>{\n    const matchesText=!q || String(row.dataset.search||"").includes(q);\n    const matchesAlert=!S.f.wkendMapAlerts || row.dataset.alert==="1";\n    row.style.display=matchesText&&matchesAlert?"":"none";\n  });\n}\n\nfunction wireTabCon
---

[renderTab] coincidencias=17
 byte en turnos, incidencias, horas semanales, balance y ausencias).")}</p>\n  </div>`;\n}\n\n/* ============================= Render orchestration =================== */\nconst RENDERERS=[renderResumen,renderRestricciones,renderHoras,renderCobertura,renderBalance,renderAusencias,renderWeekends,renderMetodologia];\nfunction renderTab(){\n  TABLE_EXPORTS.clear(); TABLE_SEQ=0;\n  const F=S.frames; const panel=document.getElementById("panel");\n  panel.innerHTML = S.tab===7? renderMetodologia() : RENDERERS[S.tab](F);\n  wireTabControls();\n}\nfunction filterWeekendMapRows(){\n  const tableEl=document.getElementById("weekendMapTable"); if(!tableEl) return;\n  const q=String(S.f.wkendMapQuery||"").trim().toLowerCase();\n  tableEl.querySelectorAll("tbody tr").forEach(row=>{\n    const matchesText=!q || String(row.dataset.search||"").includes(q);\n    
---
   const matchesAlert=!S.f.wkendMapAlerts || row.dataset.alert==="1";\n    row.style.display=matchesText&&matchesAlert?"":"none";\n  });\n}\n\nfunction wireTabControls(){\n  const on=(id,ev,fn)=>{ const el=document.getElementById(id); if(el) el.addEventListener(ev,fn); };\n  on("wkDev","change",e=>{S.f.wkDev=e.target.checked;renderTab();});\n  document.querySelectorAll("#wkStatusChips .chip").forEach(c=>c.addEventListener("click",()=>{ const s=c.dataset.s; const i=S.f.wkStatus.indexOf(s); if(i>=0)S.f.wkStatus.splice(i,1); else S.f.wkStatus.push(s); renderTab(); }));\n  on("wkWeek","change",e=>{S.f.wkWeek=e.target.value;renderTab();});\n  on("wkEmp","input",e=>{S.f.wkEmp=e.target.value;renderTab();});\n  on("balNon","change",e=>{S.f.balNon=e.target.checked;renderTab();});\n  on("morningCutoff","change",e=>{S.f.morningCutoff=e.target.value||"
---
; if(el) el.addEventListener(ev,fn); };\n  on("wkDev","change",e=>{S.f.wkDev=e.target.checked;renderTab();});\n  document.querySelectorAll("#wkStatusChips .chip").forEach(c=>c.addEventListener("click",()=>{ const s=c.dataset.s; const i=S.f.wkStatus.indexOf(s); if(i>=0)S.f.wkStatus.splice(i,1); else S.f.wkStatus.push(s); renderTab(); }));\n  on("wkWeek","change",e=>{S.f.wkWeek=e.target.value;renderTab();});\n  on("wkEmp","input",e=>{S.f.wkEmp=e.target.value;renderTab();});\n  on("balNon","change",e=>{S.f.balNon=e.target.checked;renderTab();});\n  on("morningCutoff","change",e=>{S.f.morningCutoff=e.target.value||"11:00";renderTab();});\n  on("afternoonCutoff","change",e=>{S.f.afternoonCutoff=e.target.value||"14:00";renderTab();});\n  on("wkendMin","change",e=>{S.f.wkendMin=Number(e.target.value);renderTab();});\n  on("wkendMax","change",e=
---
kDev=e.target.checked;renderTab();});\n  document.querySelectorAll("#wkStatusChips .chip").forEach(c=>c.addEventListener("click",()=>{ const s=c.dataset.s; const i=S.f.wkStatus.indexOf(s); if(i>=0)S.f.wkStatus.splice(i,1); else S.f.wkStatus.push(s); renderTab(); }));\n  on("wkWeek","change",e=>{S.f.wkWeek=e.target.value;renderTab();});\n  on("wkEmp","input",e=>{S.f.wkEmp=e.target.value;renderTab();});\n  on("balNon","change",e=>{S.f.balNon=e.target.checked;renderTab();});\n  on("morningCutoff","change",e=>{S.f.morningCutoff=e.target.value||"11:00";renderTab();});\n  on("afternoonCutoff","change",e=>{S.f.afternoonCutoff=e.target.value||"14:00";renderTab();});\n  on("wkendMin","change",e=>{S.f.wkendMin=Number(e.target.value);renderTab();});\n  on("wkendMax","change",e=>{S.f.wkendMax=Number(e.target.value);renderTab();});\n  on("wkendReqFul
---
"#wkStatusChips .chip").forEach(c=>c.addEventListener("click",()=>{ const s=c.dataset.s; const i=S.f.wkStatus.indexOf(s); if(i>=0)S.f.wkStatus.splice(i,1); else S.f.wkStatus.push(s); renderTab(); }));\n  on("wkWeek","change",e=>{S.f.wkWeek=e.target.value;renderTab();});\n  on("wkEmp","input",e=>{S.f.wkEmp=e.target.value;renderTab();});\n  on("balNon","change",e=>{S.f.balNon=e.target.checked;renderTab();});\n  on("morningCutoff","change",e=>{S.f.morningCutoff=e.target.value||"11:00";renderTab();});\n  on("afternoonCutoff","change",e=>{S.f.afternoonCutoff=e.target.value||"14:00";renderTab();});\n  on("wkendMin","change",e=>{S.f.wkendMin=Number(e.target.value);renderTab();});\n  on("wkendMax","change",e=>{S.f.wkendMax=Number(e.target.value);renderTab();});\n  on("wkendReqFull","change",e=>{S.f.wkendReqFull=Math.max(0,Math.floor(Number(e.ta
---
st s=c.dataset.s; const i=S.f.wkStatus.indexOf(s); if(i>=0)S.f.wkStatus.splice(i,1); else S.f.wkStatus.push(s); renderTab(); }));\n  on("wkWeek","change",e=>{S.f.wkWeek=e.target.value;renderTab();});\n  on("wkEmp","input",e=>{S.f.wkEmp=e.target.value;renderTab();});\n  on("balNon","change",e=>{S.f.balNon=e.target.checked;renderTab();});\n  on("morningCutoff","change",e=>{S.f.morningCutoff=e.target.value||"11:00";renderTab();});\n  on("afternoonCutoff","change",e=>{S.f.afternoonCutoff=e.target.value||"14:00";renderTab();});\n  on("wkendMin","change",e=>{S.f.wkendMin=Number(e.target.value);renderTab();});\n  on("wkendMax","change",e=>{S.f.wkendMax=Number(e.target.value);renderTab();});\n  on("wkendReqFull","change",e=>{S.f.wkendReqFull=Math.max(0,Math.floor(Number(e.target.value)||0));renderTab();});\n  on("wkendReqSat","change",e=>{S.f.wk
---
f.wkStatus.push(s); renderTab(); }));\n  on("wkWeek","change",e=>{S.f.wkWeek=e.target.value;renderTab();});\n  on("wkEmp","input",e=>{S.f.wkEmp=e.target.value;renderTab();});\n  on("balNon","change",e=>{S.f.balNon=e.target.checked;renderTab();});\n  on("morningCutoff","change",e=>{S.f.morningCutoff=e.target.value||"11:00";renderTab();});\n  on("afternoonCutoff","change",e=>{S.f.afternoonCutoff=e.target.value||"14:00";renderTab();});\n  on("wkendMin","change",e=>{S.f.wkendMin=Number(e.target.value);renderTab();});\n  on("wkendMax","change",e=>{S.f.wkendMax=Number(e.target.value);renderTab();});\n  on("wkendReqFull","change",e=>{S.f.wkendReqFull=Math.max(0,Math.floor(Number(e.target.value)||0));renderTab();});\n  on("wkendReqSat","change",e=>{S.f.wkendReqSat=Math.max(0,Math.floor(Number(e.target.value)||0));renderTab();});\n  on("wkendReqSu
---
rTab();});\n  on("wkEmp","input",e=>{S.f.wkEmp=e.target.value;renderTab();});\n  on("balNon","change",e=>{S.f.balNon=e.target.checked;renderTab();});\n  on("morningCutoff","change",e=>{S.f.morningCutoff=e.target.value||"11:00";renderTab();});\n  on("afternoonCutoff","change",e=>{S.f.afternoonCutoff=e.target.value||"14:00";renderTab();});\n  on("wkendMin","change",e=>{S.f.wkendMin=Number(e.target.value);renderTab();});\n  on("wkendMax","change",e=>{S.f.wkendMax=Number(e.target.value);renderTab();});\n  on("wkendReqFull","change",e=>{S.f.wkendReqFull=Math.max(0,Math.floor(Number(e.target.value)||0));renderTab();});\n  on("wkendReqSat","change",e=>{S.f.wkendReqSat=Math.max(0,Math.floor(Number(e.target.value)||0));renderTab();});\n  on("wkendReqSun","change",e=>{S.f.wkendReqSun=Math.max(0,Math.floor(Number(e.target.value)||0));renderTab();})
---

[tabs] coincidencias=7
5f;font-size:.9rem}\n  .source-box code{background:#dbeafe;padding:1px 6px;border-radius:5px;font-size:.85em}\n  .caption{color:var(--muted);font-size:.8rem;margin:8px 0 14px}\n  .empty-state{background:#fff;border:1px solid var(--line);border-radius:12px;padding:40px;text-align:center;color:var(--muted)}\n  /* Tabs */\n  .tabs{display:flex;flex-wrap:wrap;gap:4px;border-bottom:2px solid #e2e8f0;margin-bottom:18px}\n  .tab{padding:9px 15px;font-size:.85rem;font-weight:600;color:var(--muted);background:none;border:0;border-bottom:3px solid transparent;cursor:pointer;margin-bottom:-2px}\n  .tab:hover{color:var(--ink)}\n  .tab.active{color:var(--blue);border-bottom-color:var(--blue)}\n  .panel{display:none}\n  .panel.active{display:block}\n  /* KPI cards */\n  .kpi-grid{display:grid;gap:12px;margin-bottom:18px}\n  .kpi-card{border-radius:14px;pa
---
ith an issue","Incumplen fines completos":"Fail full-weekend rule","Incumplen sábados":"Fail Saturday rule","Incumplen domingos":"Fail Sunday rule",\n  "Incidencias según las reglas introducidas":"Issues under the entered rules","Las pestañas se recalculan inmediatamente al cambiar cualquiera de los tres mínimos.":"The tabs recalculate immediately when any of the three minimums changes.",\n  "Fines completos":"Full weekends","Sábados":"Saturdays","Domingos":"Sundays","Mostrar solo empleado-mes con alguna incidencia":"Show only employee-month rows with an issue",\n  "Las semanas completamente vacías de planificación se excluyen de cobertura, ausencias, balance y fines de semana.":"Completely empty planning weeks are excluded from coverage, absences, balance and weekend analysis.","Reglas de fin de semana":"Weekend rules","fines comp
---
"0 days","1 día":"1 day","2 días":"2 days",\n  "Diagnóstico rápido de empleados sin fin de semana completo":"Quick diagnosis of employees with no full weekend off",\n  "Las pestañas parten únicamente de empleados que no tienen ningún sábado y domingo libres en el mismo fin de semana durante el periodo analizado.":"These tabs only cover employees who never have both Saturday and Sunday off on the same weekend during the analysed period.",\n  "Todos":"All","Sin sábados":"No Saturdays off","Sin domingos":"No Sundays off","Sin ningún día":"No day off",\n  "No hay empleados en esta categoría.":"No employees in this category.",\n  "Tienda":"Store","Fines completos libres":"Full weekends off","Sábados libres":"Saturdays off","Domingos libres":"Sundays off",\n  "Resumen empleado-mes":"Employee-month summary","Mostrar solo empleado-mes con alg
---
 wk-${v}">${v}</span>`}</td>`; });\n      h+=`<td>${t("Fines completos: {n} de {total}",{n:row.full,total:order.length})}</td></tr>`;\n    });\n    h+=`</tbody></table></div>`+legend([{label:t("0 días"),color:"#d8e1ec"},{label:t("1 día"),color:"#8dbcf6"},{label:t("2 días"),color:"#244ed8"}])+`</div>`;\n  }\n  // diagnostic tabs\n  h+=`<div class="block"><h3>${t("Incidencias según las reglas introducidas")}</h3>`+help(t("Las pestañas se recalculan inmediatamente al cambiar cualquiera de los tres mínimos."));\n  const diagSets=[[t("Todas"),nAny],[t("Fines completos"),nWk],[t("Sábados"),nSat],[t("Domingos"),nSun]];\n  h+=`<div class="chips" id="wkendDiag">`+diagSets.map((d,i)=>`<span class="chip ${S.f.wkendDiag===i?"on":""}" data-i="${i}">${d[0]} (${d[1].length})</span>`).join("")+`</div>`;\n  const cur=diagSets[S.f.wkendDiag][1];\n  if(!cur
---
("wkendMapAlerts","change",e=>{S.f.wkendMapAlerts=e.target.checked;filterWeekendMapRows();});\n  document.querySelectorAll("[data-table-export]").forEach(btn=>btn.addEventListener("click",()=>downloadTableXlsx(btn.dataset.tableExport)));\n  filterWeekendMapRows();\n}\n\nfunction renderContent(){\n  const src=S.source;\n  let tabsHtml=`<div class="tabs">`+tabNames().map((n,i)=>`<button class="tab ${i===S.tab?"active":""}" data-i="${i}">${n}</button>`).join("")+`</div><div id="panel" class="panel active"></div>`;\n  document.getElementById("content").innerHTML=tabsHtml;\n  document.querySelectorAll(".tab").forEach(t=>t.addEventListener("click",()=>{ S.tab=Number(t.dataset.i); document.querySelectorAll(".tab").forEach(x=>x.classList.toggle("active",x===t)); renderTab(); }));\n  renderTab();\n}\n\n/* ============================= Sidebar / load 
---
nge",e=>{S.f.wkendMapAlerts=e.target.checked;filterWeekendMapRows();});\n  document.querySelectorAll("[data-table-export]").forEach(btn=>btn.addEventListener("click",()=>downloadTableXlsx(btn.dataset.tableExport)));\n  filterWeekendMapRows();\n}\n\nfunction renderContent(){\n  const src=S.source;\n  let tabsHtml=`<div class="tabs">`+tabNames().map((n,i)=>`<button class="tab ${i===S.tab?"active":""}" data-i="${i}">${n}</button>`).join("")+`</div><div id="panel" class="panel active"></div>`;\n  document.getElementById("content").innerHTML=tabsHtml;\n  document.querySelectorAll(".tab").forEach(t=>t.addEventListener("click",()=>{ S.tab=Number(t.dataset.i); document.querySelectorAll(".tab").forEach(x=>x.classList.toggle("active",x===t)); renderTab(); }));\n  renderTab();\n}\n\n/* ============================= Sidebar / load ======================
---
  filterWeekendMapRows();\n}\n\nfunction renderContent(){\n  const src=S.source;\n  let tabsHtml=`<div class="tabs">`+tabNames().map((n,i)=>`<button class="tab ${i===S.tab?"active":""}" data-i="${i}">${n}</button>`).join("")+`</div><div id="panel" class="panel active"></div>`;\n  document.getElementById("content").innerHTML=tabsHtml;\n  document.querySelectorAll(".tab").forEach(t=>t.addEventListener("click",()=>{ S.tab=Number(t.dataset.i); document.querySelectorAll(".tab").forEach(x=>x.classList.toggle("active",x===t)); renderTab(); }));\n  renderTab();\n}\n\n/* ============================= Sidebar / load ========================= */\nfunction selectSource(src){\n  S.source=src; S.tab=0; S.weekIdx=-1;\n  S.f={ wkDev:true, wkStatus:null, wkWeek:"Todas", wkEmp:"", balNon:true, morningCutoff:"11:00", afternoonCutoff:"14:00", wkendMin:null, wken
---
```
